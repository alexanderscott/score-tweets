var assert = require('assert'),
    NHLScore = require('../lib/nhl');

describe('NHLScore', function(){
    
    before(function(cb){
        cb();
    });

    beforeEach(function(cb){
        cb();
    });

    afterEach(function(cb){
        cb();
    });

    after(function(cb){
        cb();
    });

});
